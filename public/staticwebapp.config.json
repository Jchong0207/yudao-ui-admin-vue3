{
  "routes": [
    {
      "route": "/user",
      "rewrite": "/user"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/static/*",
      "/api/*"
    ]
  }
}